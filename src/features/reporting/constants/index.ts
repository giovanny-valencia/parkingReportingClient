/**
 * Defines actions related to user support features.
 */
export enum SupportAction {
  GuideAndFAQ = "GuideAndFAQ",
  Account = "Account",
  ContactUs = "ContactUs",
}

/**
 * Defines the main service categories or report types that the user can initiate.
 */
export enum ServiceAction {
  VehicleReport = "VehicleReport",
  InfrastructureReport = "InfrastructureReport",
}
